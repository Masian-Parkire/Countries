import { PopulationCountsDTO } from './population-counts-dto';

describe('PopulationCountsDTO', () => {
  it('should create an instance', () => {
    expect(new PopulationCountsDTO()).toBeTruthy();
  });
});
